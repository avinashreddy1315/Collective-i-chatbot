# where new uploads (and your existing .txts) live:
DATA_FOLDER=../data

# where Chroma will persist its vector DB:
CHROMA_PERSIST_DIR=../chromadb

# embedding model to use
EMB_MODEL=all-MiniLM-L6-v2
